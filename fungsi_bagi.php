function bagi(n, d)
{
    if d = 0 then
    error(DivisionByZero) 
    end
        if d < 0 then (Q, R) :=
            divide(n, -d);
        return (-q, r)
    end
        if n < 0 then
            (Q, R) := divide(-N, D)
        if R = 0 then return (-Q, 0)
            else return(-Q - , D - R)
        end
    end
}
